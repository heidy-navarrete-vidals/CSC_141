# This will contain an intentional error
colors = ["blue", "red", "yellow", "green", "purple"]

# Intentional error: print(colors[5])

#This code will not work as there are only 4 items in the color list because it starts at 0, not 1.

#The correct code:

print(colors[4])